apiVersion: v1
kind: Service
metadata:
  name: cert-operator
  namespace: {{ .Values.namespace }}
  labels:
    app: cert-operator
    version: {{ .Chart.Version }}
  annotations:
    prometheus.io/scrape: "true"
spec:
  ports:
  - port: 8000
  selector:
    app: cert-operator
    version: {{ .Chart.Version }}
